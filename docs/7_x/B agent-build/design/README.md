# HMP Agent 设计资料 · 索引

> **本目录**：`docs/7_x/B agent-build/design/` —— 设计资料（要建成什么样）
> **姊妹目录**：`../taskbook/` —— 推进计划与验收档案（做到哪了）

## 两个目录的分工

| | `design/` | `taskbook/` |
|---|---|---|
| **回答什么** | 要建成什么样 | 做到哪了 |
| **内容** | 总纲、架构、各子系统方案与规范 | 生命线、阶段族章节、验收结果与踩坑 |
| **组织方式** | 按主题（一个子系统一份） | 按阶段族（`f1`–`f9`，一族一章） |
| **变更频率** | 决策时才动 | 每阶段推进都动 |
| **冲突时** | `agent.md` 为单一事实来源 | 以实际提交与验收为准 |

一句话：**方案定了写进 `design/`，实施的结果记进 `taskbook/`。**

***

## 文件索引

### 核心

| 文件 | 内容 |
|------|------|
| [`agent.md`](agent.md) | **设计总纲 · 单一事实来源**。定位 / 本体 / 认识论 / 能力面 / 交互实施 / 工程。冲突时以此为准 |
| [`agent-architecture.md`](agent-architecture.md) | 架构详解：设计铁则 F1-F6、两层结构（每 Agent 预算 + 全局调度器）、各 Agent 详解、源码位置索引、**可见性与接口面**（F13 的设计依据：判据 / 验收口径 / 编译反推法） |

### 用户认识模块（画像）

| 文件 | 内容 |
|------|------|
| [`agent-profile.md`](agent-profile.md) | **用户认识模块的唯一依据（v3.8）**。两层结构（侧写层 = 预设骨架 + 槽位闭集 + 证据反链／证据层 = 可审计事实行）、**分级记忆 L1-L3 与跨层流动规则**、认知准入三档（A 可硬确证 / B 可弱确证降级表达 / C 明确不做，含**拒绝 MBTI 的论证**）、C2 修订、信息来源三档、验收剧本 P1-P21、待拍板 PF1-PF12 |

> 本模块认领了架构文档里两处**悬空承诺**：`agent-architecture.md:130` 的「Recall 偏好画像」与组件图的 `AgentMemory（持久化，跨会话）`
> —— 两者此前均无代码实现（全仓 `AgentMemory` / `agent_memory` / `UserProfile` / `SeedProfile` 零命中），
> 以及 `f5` 登记却未闭环的「Feedback → Recall → 推荐闭环」。登记为 **F9-T0**，**已于 2026-09-16 全量落地**（归属收编 `MasterAgent`，定名 `UserMemory`）。

### 电台子系统

| 文件 | 内容 |
|------|------|
| [`agent-radio.md`](agent-radio.md) | **电台 agent 的唯一依据**。契约、观测面、行动面、验收剧本 S1-S7、决议 C1-C5、决策内核；附录 A 收设计演进（观测层断链根因 + 五回合→三动作的决策轨迹 + 未落地清单） |
| [`agent-radio-console.md`](agent-radio-console.md) | **电台控制台（长按伙伴胶囊唤起的全屏面板）的唯一依据**。手势与状态机、面板结构（头部 / 在播 / 节目单 / 折叠区 / 吸底收档）、滚动与关闭的手势分工、「收档 ≠ 断电」的语义、与 C1·C3 的关系、待拍板 D1-D3。**状态：设计待拍板（2026-09-19），未实施** |

> 本文件合并自原四份电台文档（`spec` / `interaction-model` / `persona` / `react`），
> 三份过程稿已被正文取代，其**仍然成立的部分**（观测层断链的推理链、决策轨迹、未落地清单）收进附录 A，其余不再保留。

### W 阶段（Hello 呈现）

| 文件 | 内容 |
|------|------|
| [`agent-w.md`](agent-w.md) | W 阶段全景：**W0** 引擎侧 `HelloSubAgent` 完整实现 → **W1** 页面级改造（P1-P5）→ **W2** 组件级改造（C1-C11），末尾附 **W 缺口登记 G1-G16** 状态表 |

> 本文件合并自原四份 W 文档（`hello` / `w1` / `w2` / `w-gaps`）；G1-G16 由原逐条长文压缩为状态表，
> 只保留「待补项 / 优先级 / 归属层 / 状态」四列，证据与验收标准见源码与 `../taskbook/f7-hello呈现.md`。

### 运行时 / 生命周期

| 文件 | 内容 |
|------|------|
| [`agent-lifecycle.md`](agent-lifecycle.md) | **agent 体系生命周期与后台存活的唯一依据**。两条根因（无独立生命周期所有者 / Android 播放服务仅 bind-only 非前台）+ 一条已决边界（电台零持久化，**刻意不落盘**）、`AgentRuntime` 所有者 + `AgentKeepAlive` 平台保活抽象、三端实现、分阶段迁移 L1-L5（L4 已撤销）、待决策 D1-D5 |
| [`agent-token.md`](agent-token.md) | **token 计量 / 上下文窗口 / 日配额 / 成本可见的唯一依据**。六个断层（真 usage 无上报通道、记账只覆盖 ReActLoop、窗口占用与日消耗口径混淆、日配额设置是假控件、窗口按 Agent 硬编码、压缩空转）、§1.1 实测峰值（1–3 万 token）、单一计量源 `TokenMeter` 架构、四阶段 T1–T5、**窗口 = 固定 64K 常量**（不做探测的逐家事实核查留档）、验收剧本 S1–S9、决策记录 D1–D6（D3 定 / D4 定 / **D6 作废**）。**状态：方案已定稿（2026-09-19），未实施**；阶段推进见 [`../taskbook/f12-token计量与窗口.md`](../taskbook/f12-token计量与窗口.md) |

> 本文件修正 `agent-radio.md` C3「退后台不算关闭，会话保留」背后的**未落实假设**——它假设"前台播放服务会保住进程"，
> 而该假设在 Android 上从未成立（播放服务实际只是 `BIND_AUTO_CREATE` 绑定，正常路径从不 `startService`）。

### 其他

| 文件 | 内容 |
|------|------|
| [`agent-g6-recommend-design.md`](agent-g6-recommend-design.md) | G6 首页双推荐页设计规格（每日推荐 + 私人推荐） |

### 工程 / 构建

| 文件 | 内容 |
|------|------|
| [`build-tasks-plan.md`](build-tasks-plan.md) | **构建任务体系补齐方案（已实施 2026-09-15）**。诊断原 10 个自定义 Gradle 任务的错位问题（测试链路断裂 / 内存参数未固化 / 无发版预检），落地 16 个新任务（总 26 个）+ CI 修复 + 低内存包装脚本；含实施验收实况与实测内存参数结论 |

> **ktlint 方案已移出本目录** → [`docs/ktlint-integration.md`](../../../ktlint-integration.md)（暂缓 —— 等 agent 分支线合并后再启动）。
> 它与 `build-tasks-plan.md` 是配套项，但性质是**全仓工程规范**而非 Agent 线设计，故归入 `docs/` 顶层，与 `VERSIONING.md` / `ci-pipeline-diagnosis.md` 并列。**本目录只收 Agent 体系的设计资料。**

***

## 阅读顺序建议

1. **先读** [`agent.md`](agent.md) —— 建立整体认知
2. **再看** [`agent-architecture.md`](agent-architecture.md) —— 理解铁则与两层结构
3. **按需要** 进子系统（电台 / W 呈现 / G6）
4. **想知道做到哪了** → 去 [`../taskbook/`](../taskbook/README.md)

***

## 维护约定

- **一个主题一份文档**：同一子系统的方案收敛到单个文件，用 `##` 分节；**不保留过程稿**。被推翻的结论若要留痕，写进该文件的「附录」节，并明确标注「已被正文取代」
- **索引里列出的文件才是有效文档**：本目录平铺，不建二级子目录；新增或合并后必须同步本索引
- **移动 / 合并 / 删除**：必须同步更新本索引、`../taskbook/` 的引用，以及源码 KDoc 里 `docs/7_x/B agent-build/design/...` 形式的路径引用
- **合并历史**：2026-09-15 W 四份 → `agent-w.md`；电台四份 → `agent-radio.md`
